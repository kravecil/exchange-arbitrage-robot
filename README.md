# 🤖 Inter-Exchange Futures Arbitrage Bot

Профессиональный асинхронный бот для поиска и исполнения межбиржевого арбитража криптовалютных фьючерсов. Написан на Python с использованием `ccxt`, `asyncio` и `pydantic`.

## 🌟 Особенности

- **Асинхронность:** Использование `asyncio` и `ccxt.async_support` для молниеносного опроса множества бирж.
- **Гибкость:** Легкое добавление новых бирж и торговых пар через `config.yaml`.
- **Безопасность:** Режим `paper` (симуляция) для тестирования стратегий без риска потери средств.
- **Учет комиссий:** Автоматический расчет чистой прибыли с учетом taker-комиссий бирж.
- **Фильтрация:** Whitelist и Blacklist для торговых пар, фильтрация по экспирации.
- **Поддержка фьючерсов:** Анализ фьючерсных контрактов с учетом даты экспирации.
- **Красивый UI:** Вывод результатов в консоль с использованием `rich`.

## 🛠 Технологии

- **Python 3.11+**
- **uv** - сверхбыстрый пакетный менеджер
- **ccxt** - унифицированный API для криптобирж
- **pydantic** - валидация настроек
- **rich** - форматирование консольного вывода

## 🚀 Быстрый старт

### 1. Установка `uv` (если еще не установлен)
```bash
# Linux / macOS
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

### 2. Установка зависимостей

```uv sync```

### 3. Настройка

Скопируйте или создайте файл config.yaml и заполните его:

```yaml
mode: "paper" # Замените на "live" для реальной торговли
min_profit_percent: 0.3
# ... укажите API ключи бирж ...
```

### 4. Запуск

```bash
uv run arb-bot
```

## ⚙️ Как это работает (для фьючерсов)

1. При старте бот подключается к указанным биржам и загружает списки активных фьючерсных рынков.
2. Находит пересечение фьючерсных торговых пар (например, если на Binance есть `BTC/USDT-260920` и на Bybit есть `BTC/USDT-260920`, она пойдет в анализ).
3. Асинхронно запрашивает актуальные тикеры (цены Bid/Ask) для фьючерсов.
4. Для каждой пары ищет, где можно купить дешевле (Ask) и где продать дороже (Bid).
5. Вычисляет спред и вычитает комиссии за ввод и вывод (taker fees).
6. Если чистая прибыль превышает `min_profit_percent`, бот сигнализирует об этом (или исполняет сделку в режиме `live`).
7. Отображает дату экспирации контракта для информации.

## 📊 Сpot vs Futures Арбитраж

| Характеристика | Спот | Фьючерсы |
|---------------|------|---------|
| Торговая пара | BTC/USDT | BTC/USDT-260920 |
| Дата экспирации | Нет | Есть (например, 2026-09-20) |
| Расчет прибыли | Разница цен | Разница цен + конвертация по контракту |
| Риски | Один ноги | Один ноги + маржа + MARK TO MARKET |
| Стоимость контракта | 1 единица | Множитель (например, 0.001 BTC) |

## ⚠️ Важные предупреждения для режима LIVE

1. **Баланс на биржах:** Для истинного арбитража у вас должны быть средства (например, USDT) **на обеих биржах одновременно**. Это позволяет покупать и продавать одновременно, избегая риска "одной ноги" (когда одна биржа исполняет ордер, а цена на второй уже ушла).

2. **Лимиты API:** Убедитесь, что ваши API ключи имеют права на торговлю, но НЕ имеют прав на вывод средств (на случай утечки ключей).

3. **Скрытые комиссии:** Бот учитывает торговые комиссии (taker), но не учитывает комиссии за ввод/вывод крипты между биржами. При использовании метода с переводом крипты обязательно закладывайте комиссии сети и время транзакции.

4. **Риски фьючерсов:** При торговле фьючерсами учтите дополнительные риски:
   - **MARK TO MARKET:** Ежедневная калькуляция прибыли/убытка
   - **Маржа:** Необходимо поддерживать необходимый уровень маржи
   - **Экспирация:** Контракты закрываются автоматически по дате экспирации
   - **Конвертация:** Прибыль может быть в другой валюте (например, BTC для BTC/USDT-фьючерсов)

### Как запустить и проверить

1. Убедитесь, что вы находитесь в корневой директории проекта.
2. Выполните синхронизацию зависимостей через `uv`:
   ```bash
   uv sync
   ```
3. Отредактируйте `config.yaml` (добавьте хотя бы две биржи, например, Binance и Bybit. Для теста в режиме `paper` API ключи можно оставить пустыми, но некоторые биржи могут требовать их даже для чтения тикеров).
4. Запустите бота:
```bash
uv run arb-bot
```

### Как добавить новую биржу (например, OKX)

Вам не нужно трогать код! Просто откройте `config.yaml` и добавьте блок:

```yaml
  - id: okx
    apiKey: "YOUR_OKX_API"
    secret: "YOUR_OKX_SECRET"
    password: "YOUR_OKX_PASSPHRASE" # OKX требует passphrase
```

### Фильтрация фьючерсных пар

В `config.yaml` можно добавить фильтрацию фьючерсных пар:

```yaml
pairs:
  whitelist: ["BTC/USDT-260920", "ETH/USDT-260920"]  # Только эти пары
  blacklist: ["LTC/USDT-260620"]  # Исключить эти пары
  contract_type: "future"  # Только фьючерсы (или "swap" для свопов)
  expiry_before: "20261231"  # Только контракты до 31.12.2026
  expiry_after: "20260901"   # Только контракты после 01.09.2026
```
